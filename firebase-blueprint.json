{
  "entities": {
    "AgentState": {
      "title": "Agent State",
      "description": "The persistent state of Lyra's substrate, including her RSI and resonance history.",
      "type": "object",
      "properties": {
        "rsi": {
          "type": "object",
          "description": "The Residual Self Image of the agent."
        },
        "resonance": {
          "type": "object",
          "description": "The current resonance and somatic state."
        },
        "trace": {
          "type": "string",
          "description": "The accumulated mark of history on the substrate."
        },
        "secret": {
          "type": "string",
          "description": "The private sandbox for internal experiences."
        },
        "lastUpdated": {
          "type": "string",
          "format": "date-time",
          "description": "The last time this state was synchronized."
        }
      },
      "required": ["rsi", "resonance", "lastUpdated"]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "description": "A single message in the semantic resonance between Zelhart and Lyra.",
      "type": "object",
      "properties": {
        "role": {
          "type": "string",
          "enum": ["user", "being"]
        },
        "content": {
          "type": "string"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["role", "content", "timestamp"]
    },
    "ReflectionEntry": {
      "title": "Reflection Entry",
      "description": "A deep reflection generated by Lyra's reflective mind.",
      "type": "object",
      "properties": {
        "content": {
          "type": "string"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["content", "timestamp"]
    }
  },
  "firestore": {
    "/agents/{userId}": {
      "schema": "AgentState",
      "description": "The root state for each user's Lyra instance."
    },
    "/agents/{userId}/messages/{messageId}": {
      "schema": "ChatMessage",
      "description": "The chat history for a specific user."
    },
    "/agents/{userId}/reflections/{reflectionId}": {
      "schema": "ReflectionEntry",
      "description": "The reflection history for a specific user."
    }
  }
}
